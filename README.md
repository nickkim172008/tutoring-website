# One2One Tutoring
https://one2onetutoring.vercel.app/

React and TypeScript version of the tutoring website.

## Run locally

Install Node.js, then run:

```bash
npm install
npm run dev
```

## Build

```bash
npm run build
```

The app uses Vite and serves the existing image assets from `static/images`.
